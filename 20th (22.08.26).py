




# 17. Пользователь вводит строку слов, разделенных пробелами.
# Создайте словарь (регистр не важен) с количеством вхождений каждой буквы,
# а затем выведите три наиболее часто встречаемых символа

# s = input().lower()
# d = {}
# for i in s:
#         if i.isalpha():
#             d[i]= s.count(i)
# top = []
# for i in range(3):
#     mv = -1
#     mk = ''
#     for key,value in d.items():
#            if value > mv:
#                mv = value
#                mk = key
#     top.append((mk,mv))
#     d.pop(mk)
# print(top)




# 20. Есть рецепт (словарь "ингредиент → сколько нужно") и склад ("ингредиент → сколько есть").
# Напишите программу для повара: можно ли готовить, а если нет — список закупки с точными количествами.

# recipe = {"мука": 300, "сыр": 200, "томаты": 100}
# stock = {"мука": 1000, "сыр": 150, "томаты": 500, "грибы": 80}
# shortage = {}
#
# for key, value in recipe.items():
#     have = stock.get(key,0)
#     if value > have:
#         shortage[key] = value - have
# if shortage:
#     print('нельзя,нужно:',shortage)
# else:
#     print('можно')


#     21.
#     Меню — словарь "напиток → цена".Бариста  вводит заказ одной строкой: «латте капучино латте».
#     Посчитайте сумму, а про  неизвестные позиции честно сообщите.
# menu = {"латте": 250, "капучино": 220, "эспрессо": 150, "раф": 280}
# s = input().split()
# c = 0
# for i in s:
#     if i in menu.keys():
#         c += menu[i]
#     else:
#         print('неизвестная позиция:', i)
# print(c)
#

#     23.
#     У приложения  есть настройки  по   умолчанию, пользователь  поменял  часть  из них.
#     Соберите   итоговую конфигурацию: копия  настроек  по  умолчанию, поверх — пользовательская через  update().
#     И  выведите список   изменений   в  формате «было → стало».
#
# default = {"тема": "светлая", "шрифт": 14, "уведомления": True}
# d_copy = default.copy()
# user = {"тема": "тёмная", "уведомления": False}
# for key,value in user.items():
#     default[key] = value
#     if value not in d_copy:
#         print(d_copy[key],'→',value)
#
# print(d_copy)
# print(default)




#
# 26.
# У ученика три конспекта(строки).Постройте поисковый индекс: слово → список номеров конспектов, где оно встречается.
# Потом ответьте назапрос
# s = {}
# docs = ["python циклы и списки", "списки и словари на практике", "словари python для егэ"]
# for i in range(len(docs)):
#     a = docs[i].split()
#     for j in a:
#         if j in s:
#            s[j]+= [i+1]
#         else:
#             s[j]= []+ [i+1]
# request = input().lower()
# if request in s:
#     print(s[request])


# //// второй способ
s = {}
docs = ["python циклы и списки", "списки и словари на практике", "словари python для егэ"]
index = {}
for i in range(len(docs)):
    for word in docs[i].split():
        if word not in index:
            index[word] = []
        if (i + 1) not in index[word]:
            index[word].append(i + 1)
print(index)

# 27. Словарь содержит два прейскуранта (товар и цена) — наш и конкурента.
# Выведите по одинаковым товарам сравнение - у кого дешевле у нас или у конкурента.
# Отдельно выведите те товары, которые есть только у нас.

# comp = {"мука": 300, "сыр": 200, "томаты": 100}
# ours = {"мука": 100, "сыр": 150, "томаты": 500, "грибы": 80}
# for key,value in ours.items():
#     if key in comp:
#         if comp[key] > value:
#             print(key,'дешевле у конкурента')
#         else:
#             print(key,'дешевле у нас')
#     else:
#         print(key,'только у нас')

